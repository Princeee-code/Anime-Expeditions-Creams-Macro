name: CI

# Sanity-checks every push/PR: runs the unit test suite, verifies that every
# Python file still parses and lints clean, that the frontend JS still parses,
# and that dependencies still resolve on Windows (the only platform this macro
# actually targets -- pywebview's WebView2 backend, the `keyboard` global-hotkey
# hooks, and core/window.py's ctypes calls are all Windows-only, so a Linux
# runner would give a false sense of coverage).
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  sanity-check:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # need real history to list every commit in this push, not just HEAD (see the Discord step below)

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install dev dependencies
        run: pip install -r requirements-dev.txt

      - name: Check code style with ruff
        run: python -m ruff check .

      - name: Run unit test suite
        run: python -m pytest tests/ -v

      - name: Compile-check every Python file
        shell: bash
        run: |
          set -e
          find . -name "*.py" -not -path "./.git/*" -print0 | xargs -0 python -m py_compile

      - name: Syntax-check the frontend
        shell: bash
        run: node --check ui/app.js
